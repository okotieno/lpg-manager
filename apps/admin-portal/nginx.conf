server {
    listen 80;
    root /usr/share/nginx/html/lpg_manager_admin_portal;

    # Handle all routes, defaulting to the dashboard folder
    location / {
        try_files $uri $uri/ /index.html;
        index index.html index.htm;
    }
}
