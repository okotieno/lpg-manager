extend type Mutation {
  assignRoleToUser(
    userId: String!
    roles: [SelectCategory]!
  ): SuccessResponse

  givePermissionsToRole(
    roleId: String
    permissions: [SelectCategory]
  ): SuccessResponse
}
